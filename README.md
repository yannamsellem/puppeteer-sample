## How it works

#### Prerequisites

Node v12.x+

Make sur you have `yarn` installed

otherwise install it with:

```bash
$ npm install -g yarn
```

#### Install dependencies

```bash
$ yarn
```

#### Run the project

```bash
$ yarn start
```
